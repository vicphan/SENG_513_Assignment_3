// Represents the game header 
// ID used for css styling 
const Header = ({id}) => {
    return (
      <header id={id} className="header">
          <h1>Dots & Boxes</h1>
      </header>
    )
  }
  
export default Header;
